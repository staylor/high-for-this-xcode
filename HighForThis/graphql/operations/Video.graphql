query Video($slug: String!) {
    video(slug: $slug) {
        dataId
        id
        slug
        thumbnails {
            height
            url
            width
        }
        title
    }
}
