query Shows($term: String, $taxonomy: String) {
	shows(latest: true, first: 200, term: $term, taxonomy: $taxonomy) {
		edges {
			node {
				id
				title
				date
				artist {
					id
					name
				}
				venue {
					id
					name
				}
			}
		}
	}
}
